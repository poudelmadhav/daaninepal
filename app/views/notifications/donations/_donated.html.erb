<li class="<%= "dropdown-item #{"unread" if !notification.read_at?}" %>"><%= link_to donorform_path(notification.notifiable.donorform_id, anchor: dom_id(notification.notifiable)), class: "dropdown-item" do %>
  <%= notification.actor.name %> has made a donaton of Rs.<%= notification.notifiable.donation_amount %>.<br>
  <span class="timestamp"><%= distance_of_time_in_words(DateTime.now, notification.created_at) %> ago</span>
  <!--  <%= notification.notifiable_type.underscore.humanize.downcase %> -->
<% end %></li>
